export const Setting = {
  PlacesCount:  312
};

export enum AppRoute {
Login = '/login',
Offers = '/offer',
Favorites = '/favorites',
Root = '/'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
